#include <iostream>
using namespace std;

int main() {
	char ch;
	if (ch == 'e') {
		cout << "동쪽으로 이동" << endl;
	}
	else if (ch == 'w') {
		cout << "서쪽으로 이동" << endl;
	}
	else if (ch == 's') {
		cout << "남쪽으로 이동" << endl;
	}
	else if (ch == 'n') {
		cout << "북쪽으로 이동" << endl;
	}
}